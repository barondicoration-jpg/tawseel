{
  "name": "agon-agent",
  "version": "1.0.0",
  "description": "Delivery & Collection Management System",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm install --prefix front-end && npm run build --prefix front-end",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix front-end",
    "seed": "npm run seed --prefix server"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.9.4"
  },
  "engines": {
    "node": ">=18.x"
  }
}
